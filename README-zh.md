# OpenMAIC 课程生成质量诊断研究版

本仓库 README 已收敛为研究分支说明。请阅读 [README.md](./README.md) 获取：

- 环境安装与仓库根目录检查方法；
- LLM API / mock 模式配置；
- `npm run eval:course:sample` 和自定义课程诊断命令；
- `/eval/runs` 报告页面与 `/eval/compare` 对比页面使用方式；
- report、trace、cache 保存位置；
- 生成后自动诊断、手动评测 classroom job、人工确认 issue 与 CSV 导出；
- 后续如何接入 OpenMAIC 原有课程生成结果。
